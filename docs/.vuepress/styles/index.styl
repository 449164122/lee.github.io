.zfs-btns {
  display: flex;
  flex-wrap: wrap;
}
.zfs-button {
  margin:10px;
}
.zfs-badge {
  margin: 16px;
  .child {
    width: 40px;
    height: 40px;
    background: #f2f3f5;
    border-radius: 4px;
  }
}
